# [数据层|A类] Redis: 会话缓存 + (阶段5)网关全局限流计数器
architecture: replication               # standalone/replication; 学习环境选哪个?限流计数器丢了会怎样?
auth:
  enabled: true                  # 关=省事,开=多一步Secret; 写下你的取舍
master:
  persistence: {size: 4Gi}
  resources:
    requests: {cpu: 500m, memory: 1Gi}
