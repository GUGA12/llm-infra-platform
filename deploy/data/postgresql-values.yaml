# [数据层|A类] PostgreSQL (bitnami)
auth:
  database: chatdb
  username: chat
  password: 123456                 # 阶段7换existingSecret; 先跑通并在此注明技术债
primary:
  persistence: {size: 8Gi}      # 依据: 审计日志增长估算, 学习环境8Gi够
  resources:
    requests: {cpu: 500m, memory: 1G}
# 走pgvector省Milvus: image.repository=pgvector/pgvector, tag=pg16
# 并加 primary.initdb.scripts 执行 CREATE EXTENSION vector;
